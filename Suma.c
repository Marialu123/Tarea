#include <stdio.h>

int main()
{
  int a,b,suma;
  printf("\n ingrese numero a:");
  scanf( "%d", &a);
  printf( "\n ingrese numero b:");
  scanf( "%d", &b);
  printf("\n la suma es: %d", a + b );
  return 0;
}

  
